# Results Directory

- Experiment logs and metrics are written here by `ExperimentLogger`.
- Each run produces a JSON file under `project/results/<experiment_id>/`.
- A CSV index is maintained at `project/results/experiments.csv`.

Example:

```
project/results/
  exp002/
    run-20250101T101500Z.json
  experiments.csv
```

